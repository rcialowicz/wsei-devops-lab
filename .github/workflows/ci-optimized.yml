name: CI - Optimized

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend:
    name: Backend (.NET)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lab5/sample-ci/backend
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('lab5/sample-ci/backend/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      # POPRAWKA: Wskazujemy konkretne projekty do przywrócenia
      - name: Restore dependencies
        run: |
          dotnet restore ProductApi/ProductApi.csproj
          dotnet restore ProductApi.Tests/ProductApi.Tests.csproj

      # POPRAWKA: Budujemy konkretny projekt
      - name: Build
        run: dotnet build ProductApi/ProductApi.csproj --no-restore --configuration Release

      # POPRAWKA: Testujemy konkretny projekt testowy
      - name: Test
        run: dotnet test ProductApi.Tests/ProductApi.Tests.csproj --no-build --configuration Release --logger trx

      # POPRAWKA: Publikujemy konkretny projekt API
      - name: Publish
        run: dotnet publish ProductApi/ProductApi.csproj --no-build --configuration Release --output ./publish

  frontend:
    name: Frontend (Node.js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lab5/sample-ci/frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: lab5/sample-ci/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
